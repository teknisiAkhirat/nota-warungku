import test from'node:test';import assert from'node:assert/strict';import{operationalDate,calculateTotal,matchesHistory,receiptText,snapshotItems}from'../src/core.js';
test('02:00 belongs to previous operational day',()=>assert.equal(operationalDate('2026-09-25T02:00:00+07:00'),'260924'));
test('05:00 starts new operational day',()=>assert.equal(operationalDate('2026-09-25T05:00:00+07:00'),'260925'));
test('23:59 keeps current operational day',()=>assert.equal(operationalDate('2026-09-25T23:59:00+07:00'),'260925'));
test('total is integer sum of subtotals',()=>assert.equal(calculateTotal([{price:10000,qty:2},{price:3000,qty:1}]),23000));
test('snapshot preserves master values',()=>assert.deepEqual(snapshotItems([{id:'m1',name:'Nasi',category:'Makanan',price:10000,qty:2}]),[{id:'m1',name:'Nasi',category:'Makanan',price:10000,qty:2,subtotal:20000}]));
test('history search matches receipt, date and menu',()=>{const t={number:'260925001',createdAt:'2026-09-25T10:00:00+07:00',items:[{name:'Tempe'}]};assert.equal(matchesHistory(t,'260925001'),true);assert.equal(matchesHistory(t,'25/09/2026'),true);assert.equal(matchesHistory(t,'tempe'),true);assert.equal(matchesHistory(t,'kopi'),false)});
test('receipt text contains closing phrase',()=>assert.match(receiptText({number:'260925001',createdAt:'2026-09-25T10:00:00+07:00',items:[{name:'Tempe',qty:2,price:2000,subtotal:4000}],total:4000}),/Terima kasih telah berbelanja/));