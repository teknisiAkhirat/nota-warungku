import test from'node:test';import assert from'node:assert/strict';function operationalDate(d){const x=new Date(d);if(x.getHours()<5)x.setDate(x.getDate()-1);return x.toISOString().slice(2,10).replaceAll('-','')}test('02:00 belongs to previous operational day',()=>assert.equal(operationalDate('2026-09-25T02:00:00+07:00'),'260924'));test('05:00 starts new operational day',()=>assert.equal(operationalDate('2026-09-25T05:00:00+07:00'),'260925'));test('sequence is three digits',()=>assert.equal(String(7).padStart(3,'0'),'007'));test('total is sum of subtotals',()=>assert.equal([{p:10000,q:2},{p:3000,q:1}].reduce((a,x)=>a+x.p*x.q,0),23000));