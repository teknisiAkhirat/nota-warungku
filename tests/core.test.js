import test from'node:test';import assert from'node:assert/strict';function operationalDate(d){const tz=new Date(new Date(d).toLocaleString('en-US',{timeZone:'Asia/Jakarta'}));if(tz.getHours()<5)tz.setDate(tz.getDate()-1);return String(tz.getFullYear()).slice(2)+String(tz.getMonth()+1).padStart(2,'0')+String(tz.getDate()).padStart(2,'0')}test('02:00 belongs to previous operational day',()=>assert.equal(operationalDate('2026-09-25T02:00:00+07:00'),'260924'));test('05:00 starts new operational day',()=>assert.equal(operationalDate('2026-09-25T05:00:00+07:00'),'260925'));test('sequence is three digits',()=>assert.equal(String(7).padStart(3,'0'),'007'));test('total is sum of subtotals',()=>assert.equal([{p:10000,q:2},{p:3000,q:1}].reduce((a,x)=>a+x.p*x.q,0),23000));